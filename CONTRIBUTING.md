# 贡献说明

当前为中文首版草稿。优先欢迎可复现的错位案例、半透明边缘问题、浏览器兼容修复和流程说明改进。

1. 修改 `src/` 后运行 `npm run build`，将两个单文件产物一起更新。
2. 运行 `npm test`、`npm run check` 与 `python3 scripts/verify_examples.py`。
3. 算法改动需附相同输入的黑底与白底对照，说明参数及损失，不只展示更好看的单张图。
4. 修改 Skill 时同步更新引用文档；不引入默认 API Key、账号池或绕过网页限制的实现。
5. 提交示例需说明来源和可公开使用范围，清理图像元数据及私人路径。

仅开发测试需要 Playwright；用户使用 HTML 无需 Node.js/Python。提交代码与文档即表示愿意按项目 GPL-3.0-or-later 许可证贡献相应内容；图像授权另行标注。
